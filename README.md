# Drone Flight Calculator

## AI Use

Used GitHub Copilot Chat (/tests) to generate the initial unit tests for calculate_flight_time(). I reviewed the generated tests and confirmed that they cover zero payload, normal payload weights, the zero-flight-time boundary, and negative weight input. I verified that all 4 tests pass with pytest.
